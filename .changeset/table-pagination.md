---
"@marcfs31/fors-design-system": minor
---

New `TablePagination`: the footer of a paginated data table — rows-per-page `Select`, a live "from–to of count" summary, and a windowed page strip (`pageRange`, also exported) with previous/next. Pages are 1-based; all copy is overridable through `labels` for localized apps.
