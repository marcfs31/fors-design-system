---
"@marcfs31/fors-design-system": minor
---

`Button` renders `data-fors="button"` so consumer stylesheets (e.g. an app-defined extra theme) can target Fors buttons with `[data-fors="button"]` without also matching the `<button>`s Radix renders inside `Select`, `Dialog`, `Calendar` and other primitives.
