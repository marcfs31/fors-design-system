---
"@marcfs31/fors-design-system": minor
---

New `ConfirmDialog`: a controlled yes/no confirmation on top of `Dialog`, exposed as `role="alertdialog"`, with `destructive` (danger confirm, initial focus on Cancel) and `loading` states — the replacement for `window.confirm` in every Fors app.
