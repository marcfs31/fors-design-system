# Changesets

This directory tracks pending version bumps. Run `npm run changeset` after a change that should ship in the next release, then `npm run version-packages` when you're ready to bump the version and update CHANGELOG.md, and `npm run release` to build and publish.

See https://github.com/changesets/changesets for the full docs.
