---
"@marcfs31/fors-design-system": minor
---

`Button` accepts `asChild` (Radix Slot) to render its styles onto a single child element — the way to make a router `<Link>` look like a button without losing link semantics.
